# Skill Configuration & Agent Integration

Completed Lesson 2 ("Skill Configuration & Agent Integration") which covered: the four-step configuration pipeline (discovery via opencode.json → listing as available_skills in system prompt → loading via skill tool → invocation), how descriptions drive agent decisions, the permission model (true/false/allowlist), and a real example of how the agent evaluated available_skills at the start of this session.

**Key concepts:** opencode.json skills directory + permissions, available_skills injection, skill tool mechanics, description-based matching.

**Status:** complete
